# Copy to `.env` and adjust per environment.

PUBLIC_API_BASE_URL=https://api.kora.mock

# MSW mocks are ON by default (this demo has no real backend).
# Set to "false" once PUBLIC_API_BASE_URL points at a real API.
# PUBLIC_ENABLE_API_MOCKING=false
