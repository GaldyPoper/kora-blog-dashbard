# Secret used to HMAC-sign the session cookie (src/lib/server/auth.ts).
# Optional in dev — a hard-coded insecure fallback is used when unset.
# REQUIRED in production: set a long random value.
AUTH_SECRET=
